# R19 auto-apply spot-audit prep — export 10-15 cases for Stan review

## Context

The 2101 second-round reply (`directives/replies/2026-05-16-2101-r19-resolver-second-round.md`) showed **55 unanimous-all-high-confidence STRONG-MERGE cases** out of 75 sampled. Calibration recommendation: enable auto-apply gate at unanimous-all-high-MERGE.

Before enabling that gate on the corpus-wide same-line REVIEW cohort (~1700 cases), Stan needs to spot-audit a stratified sample of 10-15 cases manually. This directive produces the readable export Stan reviews at his desk.

**This is preparation work — NOT enabling auto-apply.** Auto-apply enable is a separate future directive that depends on Stan's spot-audit approval AND will itself trip §7.3 trigger #10 (discipline-shifting addition that shapes how the apparatus is operated), requiring ≥2 parallel adversarial agents before implementation.

## Items

1. **Read the 2101 agreement artifact.** Located at `C:/tmp/r19-75case-agreement.json` per the 2101 reply's "Artifacts" section. If the artifact has moved or been cleaned up, reconstruct from `C:/tmp/r19-75case-*.jsonl` run outputs (the 2101 reply enumerates agent IDs and locations).

2. **Filter to unanimous-all-high-MERGE same-line cohort.** Predicate:
   - `agreement == 'unanimous'`
   - All three runs `verdict == STRONG-MERGE`
   - All three runs `confidence == high`
   - `review_subtype == 'same-line'`

   Expected: roughly the 55-case cohort identified in 2101. Confirm count.

3. **Stratify sample to 10-15 cases:**
   - ~2 cases per major head_lemma covered in the 75-case sample: `thing`, `word`, `time`, `day`, `place`, `people`, `man` (7 lemmas × 2 = 14, target range)
   - Random within each lemma (use a reproducible seed; document it)
   - All same-line only (cross-line auto-apply is a separate later conversation)
   - If a lemma had fewer than 2 cases in the unanimous-all-high-MERGE same-line cohort, take what's available; document the shortfall and pick from the next-most-populous lemma to compensate

4. **Export readable markdown table** to `directives/replies/2026-05-16-2201-spot-audit-prep.md`. Per case, structured as:

   ```
   ### Case N: <book> <chapter>:<verse> (head: <lemma>)

   **v2-mine rendering:**
   <full multi-line rendering exactly as it appears in v2-mine>

   **Head:** <token> (line <N>)
   **Relative clause root:** <token> (line <N>)
   **Subtype:** same-line

   **Resolver verdict:** STRONG-MERGE (3/3 unanimous; all high confidence)
   **Reasoning (Run 1, representative):** <one-sentence excerpt from Run 1's reasoning>
   ```

   Stan should be able to read the v2-mine rendering, see the head + relative, and confirm in seconds whether the existing merge is correct.

5. **Sort the table by head_lemma** for scan-ability. Within each lemma, sort by book order then chapter:verse.

## Reporting

Reply at `directives/replies/2026-05-16-2201-spot-audit-prep.md`:
- The 10-15-case stratified spot-audit table (per Item 4)
- Stratification report: which lemmas hit the 2-case target, which fell short
- Any cases where the v2-mine rendering surprised the resolver's verdict (unlikely given unanimous-all-high gate, but flag if any)
- Confirmation of the seed used for random within-lemma selection

## Audit triggers

Read-only artifact preparation. No v2-mine changes. No validator changes. No closed-list additions. Audit-skippable per §7.4.

## After Stan-review

Stan will review the table at his pace. After review he will either:

(a) **Approve the auto-apply gate** → triggers a follow-on directive to implement auto-apply, which itself trips §7.3 trigger #10 and requires ≥2 parallel adversarial agents before implementation
(b) **Identify a failure pattern** → recalibrate gate (narrower lemma exclusions, additional filter signals, etc.)

That follow-on directive is intentionally NOT drafted here — it depends on Stan's spot-audit findings shaping the gate's exact parameters.
